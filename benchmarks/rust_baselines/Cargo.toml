[package]
name = "aura-rust-baselines"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
publish = false

[dependencies]
tokio = { version = "=1.53.1", features = ["rt-multi-thread", "net", "io-util", "time"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
